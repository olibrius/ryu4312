-r ../pip-requirements.txt
# eventlet>=0.35.0 required for Python 3.11/3.12 compatibility
# (greenlet and ssl fixes introduced in the 0.35.x series)
eventlet>=0.35.0
dnspython>=2.0  # required by eventlet on Python 3.10+ (collections.MutableMapping removal)
msgpack>=0.4.0  # RPC library, BGP speaker(net_cntl)
netaddr
oslo.config>=2.5.0
ovs>=2.6.0  # OVSDB
packaging>=21.0
routes  # wsgi
six>=1.4.0
tinyrpc==1.0.4  # RPC library, BGP speaker(net_cntl)
webob>=1.2  # wsgi
